/** Canonical XRay extraction result states. Product-safe; never leak provider errors. */
export const XRAY_EXTRACT_RESULTS = [
  'SUCCESS',
  'PARTIAL',
  'NO_LEGS_FOUND',
  'NEEDS_CONFIRMATION',
  'UNREADABLE_IMAGE',
  'RATE_LIMITED',
  'USER_QUOTA_EXCEEDED',
  'GLOBAL_QUOTA_EXCEEDED',
  'EXTRACTION_DISABLED',
  'AUTH_REQUIRED',
  'PROVIDER_UNAVAILABLE',
  'INTERNAL_ERROR',
  'IN_FLIGHT',
] as const;

export type XrayExtractResult = (typeof XRAY_EXTRACT_RESULTS)[number];

export const XRAY_EXTRACT_HTTP_STATUS: Record<XrayExtractResult, number> = {
  SUCCESS: 200,
  PARTIAL: 200,
  NO_LEGS_FOUND: 200,
  NEEDS_CONFIRMATION: 200,
  UNREADABLE_IMAGE: 422,
  RATE_LIMITED: 429,
  USER_QUOTA_EXCEEDED: 429,
  GLOBAL_QUOTA_EXCEEDED: 429,
  EXTRACTION_DISABLED: 503,
  AUTH_REQUIRED: 401,
  PROVIDER_UNAVAILABLE: 503,
  INTERNAL_ERROR: 500,
  IN_FLIGHT: 429,
};
