# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Stamped at package time. Empty SHA when the worktree is dirty.
projection-ledger.revision.auto.tfvars.json

# Build artifacts (zip produced by archive_file)
*.zip
tfplan
tfplan-*
*.tfplan
