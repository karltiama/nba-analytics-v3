## Context
Terraform validation failed because `infra/outputs.tf` referenced a non-existent Scheduler resource name (`aws_scheduler_schedule.player_props`).

## Goal
Fix Terraform outputs so they reference the correct player props Scheduler resources.

## Files Changed
- `infra/outputs.tf`

## Implementation Notes (key logic explained)
- Updated `player_props_schedule_name` output to resolve to:
  - `aws_scheduler_schedule.player_props_crons[0].name` when `player_props_schedule_crons` is non-empty
  - otherwise `aws_scheduler_schedule.player_props_rate[0].name`
- This matches the actual resources declared in `infra/lambda.tf`.

## Key Decisions (and alternatives considered)
- Decision: output the first schedule name for cron mode (consistent with prior single-name output intent).
- Alternative: output a list of all cron schedule names; deferred to keep output stable.

## Assumptions
- When cron list is non-empty, at least one cron schedule exists.

## Failure Modes
- If schedules are disabled, output remains null as before.

## How to Test (commands + steps)
1. Run: `terraform -chdir=infra validate`
2. Run: `terraform -chdir=infra plan` and confirm outputs resolve without errors.

## Next Steps
- Apply Terraform when ready and confirm AWS Scheduler next run times match ET cadence.
