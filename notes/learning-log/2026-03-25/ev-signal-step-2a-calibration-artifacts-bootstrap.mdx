## Context
Track A requires a calibration artifact source before runtime probability adjustment can be introduced.

## Goal
Create initial calibration artifact structure for Track A and Track B with safe identity defaults.

## Files Changed
- `lib/betting/ev-calibration-artifacts.json`

## Implementation Notes (key logic explained)
- Added versioned artifact JSON with per-track, per-prop linear calibration params.
- Bootstrap values use identity (`slope=1`, `intercept=0`) to preserve behavior until fitted values are generated.

## Key Decisions (and alternatives considered)
- Chose JSON artifacts for easy export/import from offline fitting scripts.
- Included both tracks early to avoid schema churn later.

## Assumptions
- Future fitting pipeline will overwrite this file with learned coefficients.

## Failure Modes
- If artifact keys don’t match runtime prop types, default coefficients will be used and calibration gain is lost.

## How to Test (commands + steps)
1. Load artifact from runtime helper.
2. Confirm version string and expected prop keys resolve.
3. Verify identity mapping outputs unchanged probabilities.

## Next Steps
- Add runtime calibration helper and wire it into EV calculation path.
