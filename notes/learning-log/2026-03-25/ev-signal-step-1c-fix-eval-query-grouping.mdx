## Context
The first run of the offline EV evaluation harness failed due to SQL grouping behavior.

## Goal
Fix the query so grouped stats no longer reference non-grouped helper columns.

## Files Changed
- `scripts/evaluate-ev-signal.ts`

## Implementation Notes (key logic explained)
- Updated `chosen` CTE to project only required columns rather than `select *`.
- Removed carry-through of `rn` into grouped CTE stages, eliminating Postgres aggregate error.

## Key Decisions (and alternatives considered)
- Kept the fix minimal in SQL shape to avoid changing downstream logic.

## Assumptions
- Remaining grouped columns cover all required projection fields.

## Failure Modes
- Future wildcard selects (`*`) in similar grouped CTE chains can reintroduce this class of error.

## How to Test (commands + steps)
1. Run: `npx tsx scripts/evaluate-ev-signal.ts --days 21`
2. Confirm script returns JSON summary instead of SQL error.

## Next Steps
- Use output to compare baseline vs upgraded scorecards and gate promotion decisions.
