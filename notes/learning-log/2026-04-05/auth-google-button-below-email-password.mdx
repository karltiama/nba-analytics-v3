## Context

Google OAuth was initially placed above the email/password form. The product preference is email/password first, **Continue with Google** after an **or** divider.

## Goal

- **Goal:** Move **Continue with Google** below the email and password fields on `/login` and `/signup`.
- **Inputs:** Existing `ContinueWithGoogleButton` and forms.
- **Output/Done:** Form → divider → Google; subtitle copy updated.

## Files Changed

- `app/signup/SignupClient.tsx`
- `app/login/LoginClient.tsx`

## Implementation Notes (key logic explained)

- Order: primary CTA remains **Create account** / **Sign in**; OAuth is secondary visually.
- `disabled={loading}` on the Google button still prevents double-submit while the email form is submitting.

## Verification Checklist

1. `/signup` shows email, password, primary button, then **or**, then **Continue with Google**.
2. `/login` matches the same order.
3. `?next=` still flows to Google OAuth and back through `/auth/callback`.
