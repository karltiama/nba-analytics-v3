## Context

Admin UI and API routes were causing maintenance issues; user requested removal of all admin pages.

## Goal

- **Goal:** Remove `app/admin/*` and `app/api/admin/*`; update docs/scripts that referenced them.
- **Inputs:** Three admin pages, two admin API routes.
- **Output/Done:** No `/admin` or `/api/admin` routes; docs/scripts updated minimally.

## Files Changed (deleted)

- `app/admin/game-statuses/page.tsx`
- `app/admin/odds-debug/page.tsx`
- `app/admin/bbref-data-check/page.tsx`
- `app/api/admin/bbref-data-check/route.ts`
- `app/api/admin/odds-debug/route.ts`

## Files Changed (edits)

- `docs/deployment-checklist.md` — removed admin hardening bullet.
- `scripts/test-odds-api.ts` — success message no longer points at admin URL.
- `scripts/FIXING_ROSTER_ISSUES_GUIDE.md` — roster checks via scripts only.
- `docs/SCRAPING_SUMMARY.md`, `docs/odds-implementation-checklist.md` — removed admin/API references.

## Verification Checklist

1. `npm run build` succeeds.
2. No `/admin` or `/api/admin` routes in the build output.
3. Data quality workflows use `scripts/` (e.g. `list-roster-issues.ts`, `test-odds-api.ts`) instead of admin UI.
