## Context

The standalone `/dashboard` route was unused; user requested removal.

## Goal

- **Goal:** Delete `app/dashboard` and fix navigation that pointed at `/dashboard`.
- **Inputs:** `app/dashboard/page.tsx`, links in games/players/betting profile copy.
- **Output/Done:** No `/dashboard` route; back links use `/` or `/betting` as appropriate.

## Files Changed

- `app/dashboard/page.tsx` — deleted; `app/dashboard` directory removed.
- `app/games/page.tsx` — nav → `/`, label “← Home”.
- `app/players/[playerId]/page.tsx` — not-found link → `/`.
- `app/players/[playerId]/components/PlayerHeader.tsx` — back link → `/`, “Home”.
- `app/betting/profile/page.tsx` — copy “Back to betting” (href was already `/betting`).

## Notes

- Betting UI still uses the word “Dashboard” for `/betting` (nav + subtitle); that is separate from the removed page.

## Verification Checklist

1. `npm run build` — no `/dashboard` route.
2. Games and player pages “Home” goes to `/`.
3. Profile back link text matches `/betting` destination.
