## Context
The empty Props Explorer aside only showed a short line of copy; the transition to loaded content felt abrupt.

## Goal
Use a skeleton layout that mirrors stat tabs, trend chart, and game log so the column shape stays stable before and after selection.

## Files Changed
- `components/betting/PropsExplorerPlayerPanel.tsx`

## Implementation Notes
- `PropsExplorerPlayerPanelBodySkeleton`: pill row, chart card with legend strip + area silhouette, game-log header + row lines (pulse + clip-path area).
- `PropsExplorerPlayerPanelSkeleton`: full card with header skeleton + body skeleton + one line “Select a player in the table”.
- Loading state after a player is selected reuses `PropsExplorerPlayerPanelBodySkeleton` under the real header (no duplicate header).

## Verification Checklist
- [ ] Empty sidebar shows skeleton, not only text.
- [ ] After clicking a player, header shows name while body shows skeleton until data loads.
