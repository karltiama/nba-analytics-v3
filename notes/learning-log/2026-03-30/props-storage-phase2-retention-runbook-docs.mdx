## Context
Retention requires repeatable operations, not only code changes. Team needed clear commands and verification steps.

## Goal
Document one-time and recurring 30-day pruning for `raw.player_prop_snapshots_v2`.

## Files Changed
- `lambda/player-props-snapshot/README.md`

## Implementation Notes (key logic explained)
- Added retention runbook section with:
  - Dry-run command
  - Execute command
  - Recurring schedule guidance
  - Verification SQL queries for table size and recent row distribution
- Also documented new payload control env vars in the env table (for upcoming optimization work).

## Key Decisions (and alternatives considered)
- Chosen: colocate retention docs in lambda README (operator touchpoint).
- Alternative: separate ops doc only; rejected to reduce drift between code and runbook.

## Assumptions
- Operators run cleanup off-peak and monitor size trend after each run.

## Failure Modes
- If pruning is not scheduled, table growth can resume despite hourly dedupe.
- Missing env var setup can block script execution.

## How to Test (commands + steps)
1. Open README and follow dry-run command.
2. Run execute command in staging.
3. Confirm verification SQL reflects row/size reduction.

## Next Steps
- Add explicit infra variable examples for payload control env vars.
- Implement raw JSON optional/sampled behavior in writer code.
