## Context
During QA, one Tailwind lint warning appeared in the props explorer row hover class.

## Goal
Resolve the warning so the edited UI file is lint-clean.

## Files Changed
- `app/betting/props-explorer/page.tsx`

## Implementation Notes (key logic explained)
- Replaced `hover:bg-white/[0.03]` with equivalent shorthand `hover:bg-white/3`.

## Key Decisions (and alternatives considered)
- Decision: keep behavior unchanged and only update class syntax.
  - Alternative: remove hover effect entirely; skipped because this was only formatting guidance.

## Assumptions
- Tailwind config in this project supports shorthand opacity syntax.

## Failure Modes
- If custom Tailwind parser rejects shorthand format, class would no-op.

## How to Test (commands + steps)
1. Re-run lint diagnostics for `props-explorer/page.tsx`.
2. Confirm warning is gone.
3. Hover rows in props explorer and confirm hover background still appears.

## Next Steps
- Run targeted eslint check across all edited user-system files.
