## Context

Landing sections used `mt-24` between hero → matchups → props → trending, `mt-32` before the feature cards, and default main/footer margins.

## Goal

Increase vertical spacing between sections slightly for clearer separation.

## Files Changed

- `components/landing/FeaturedGames.tsx` — section `mt-24` → `mt-32`.
- `components/landing/LandingPropsTablePreview.tsx` — `mt-24` → `mt-32`.
- `components/landing/LandingTrendingPlayerStripPreview.tsx` — `mt-24` → `mt-32`.
- `app/page.tsx` — main `pb-20` → `pb-28`; features grid `mt-32` → `mt-40`; footer `mt-20` → `mt-28`.

## Implementation Notes

- Tailwind scale: `mt-32` = 8rem, `mt-40` = 10rem (+2rem vs prior feature block gap).

## Verification Checklist

- [ ] Scroll `/` — spacing between hero, each preview section, feature cards, and footer feels roomier.
- [ ] No layout overlap or odd gaps on mobile.
